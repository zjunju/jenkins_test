pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
                sh 'ls -al'
                sh 'python'
                sh 'pwd'
                sh 'pwd'
            }
        }
    }
}
